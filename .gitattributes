# Keep tracked files byte-exact across platforms. Git's EOL normalization would
# otherwise rewrite LF to CRLF on checkout, breaking byte-exact comparison with
# ~/AGENTS.md and ~/.claude/CLAUDE.md.
* -text
