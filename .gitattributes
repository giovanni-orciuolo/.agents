# Keep tracked config byte-exact across platforms. Git's EOL normalization would
# otherwise rewrite CRLF/LF on checkout, making `claude-config.py check` report
# spurious drift between the repo and ~/.claude.
* -text
